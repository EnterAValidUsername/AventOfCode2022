#include<bits/stdc++.h>
using namespace std;

main() {
	string input;
	fin >> input;
	
	int N = input.size();
	unordered_set < char > count;
	queue < char > q;
	
	while (q.size() < 4) {
		
	}
}

