#include<bits/stdc++.h>

using namespace std;

ifstream fin ("input.txt");

main() {
	string input1, input2, input3;
	unordered_set < char > Elf1, Elf2, Elf3; // i figured out that sets are better than maps in this problem case and in the previous (3_1)
	
	while (fin >> input1 >> input2 >> input3) {
		// posso farla in N per ogni 3 input presi
	}
	
}

